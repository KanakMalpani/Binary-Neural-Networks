# Soft pins for portable installs. Prefer:
#   pip install -e ".[dev]" -c constraints.txt
#
# Exact wheels vary by OS/Python; these bounds keep third-party / other-AI
# clones in a known-good band without requiring a full lockfile.

torch>=2.1,<2.13
# macOS x86_64 only publishes older torch wheels (last ~2.2.x) built against
# NumPy 1.x — NumPy 2.x makes torch.from_numpy raise "Numpy is not available".
# Elsewhere NumPy 2 is fine with current torch; keep the same upper bound so
# one constraints file works on every runner in the portability matrix.
numpy>=1.24,<2
pytest>=9.1.1,<10
pytest-timeout>=2.4.0,<3
# Optional HF lane:
# transformers>=4.40,<5
# datasets>=2.18,<4
# huggingface_hub>=0.22,<1
